# Philip Hull — Mechanical Design

11×17 shop-drawing portfolio with live STEP assemblies.

## Vercel

- Framework Preset: **Other** (this `vercel.json` sets that)
- Build command: `npm run build`
- Output directory: leave empty
- No environment variables required (`VITE_AUTH_ENABLED=false` is in `.grok/app-env.json`)
